# dependencies
/node_modules
/.pnp
.pnp.js

# next.js
/.next/
/out/

# production
/build

# runtime data (estado de proyectos/jobs) y salidas generadas
/data/
/output/

# env
.env
.env.local
.env*.local
.env.production*

# Credenciales. El repo es PUBLICO: nada de esto puede entrar a git nunca.
CREDENCIALES-*.txt
adc.json
*-service-account.json
application_default_credentials.json

# SDK de gcloud descomprimido adentro del repo (367 MB). No es codigo del
# proyecto y no tiene por que viajar ni a git ni a la VPS.
/googlecloudxd/

# misc
.DS_Store
*.pem
npm-debug.log*
yarn-debug.log*
yarn-error.log*
*.tsbuildinfo
next-env.d.ts.bak
