"""Capa API REST del backend (routers de FastAPI).

Agrupa los routers que exponen los endpoints del contrato de la API:

* :mod:`app.api.clips` — ``POST /clips`` (subida de clips, Req 1).
* :mod:`app.api.music` — ``POST /musica`` (subida de música, Req 8.1, 8.2).

Los endpoints de procesamiento, progreso y descarga se añaden en la tarea 14.
"""
